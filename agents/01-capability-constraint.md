# 主智能体的能力约束：为什么规划者不应该有执行权限

> ⚠️ 撰写中

本文讨论在多智能体架构中，为什么主 agent（规划者）不应持有 write、edit、bash 等执行权限。核心论点：一旦主 agent 能直接执行操作，它就会倾向于"自己干"而非委派，从而破坏子 agent 隔离的架构前提。文章将描述我在 subagent-isolation 中实现硬剥夺的实践过程，包括遇到的阻力和最终的设计取舍。
